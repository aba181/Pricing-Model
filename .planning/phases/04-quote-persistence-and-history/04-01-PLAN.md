---
phase: 04-quote-persistence-and-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fastapi-project/migrations/004_create_quotes.sql
  - fastapi-project/app/quotes/__init__.py
  - fastapi-project/app/quotes/schemas.py
  - fastapi-project/app/quotes/repository.py
  - fastapi-project/app/quotes/service.py
  - fastapi-project/tests/conftest.py
  - fastapi-project/tests/test_quotes.py
autonomous: true
requirements: [QUOT-01, QUOT-02]

must_haves:
  truths:
    - "Quote table exists with JSONB snapshot columns for full app state"
    - "Quote number generation produces unique sequential numbers per client code (e.g., EZJ-001, EZJ-002)"
    - "Decimal precision is preserved in JSONB serialization (string encoding, never float)"
    - "Quote snapshots are immutable -- no UPDATE on snapshot columns"
  artifacts:
    - path: "fastapi-project/migrations/004_create_quotes.sql"
      provides: "DDL for quotes, quote_msn_snapshots, quote_sequences tables"
      contains: "CREATE TABLE IF NOT EXISTS quotes"
    - path: "fastapi-project/app/quotes/repository.py"
      provides: "QuoteRepository with create, list, get, update_status, generate_quote_number"
      exports: ["QuoteRepository"]
    - path: "fastapi-project/app/quotes/schemas.py"
      provides: "Pydantic schemas for quote CRUD with Decimal types"
      exports: ["SaveQuoteRequest", "QuoteResponse", "QuoteListItem"]
    - path: "fastapi-project/app/quotes/service.py"
      provides: "DecimalEncoder, snapshot assembly helpers"
      exports: ["DecimalEncoder"]
    - path: "fastapi-project/tests/test_quotes.py"
      provides: "Wave 0 test stubs for quote endpoints"
  key_links:
    - from: "fastapi-project/app/quotes/repository.py"
      to: "fastapi-project/app/db/base_repository.py"
      via: "class QuoteRepository(BaseRepository)"
      pattern: "class QuoteRepository\\(BaseRepository\\)"
    - from: "fastapi-project/app/quotes/repository.py"
      to: "fastapi-project/app/quotes/service.py"
      via: "DecimalEncoder for JSONB serialization"
      pattern: "DecimalEncoder"
---

<objective>
Build the quote persistence data layer: database schema, repository, Pydantic schemas, service utilities, and test infrastructure.

Purpose: Establish the foundation for saving and retrieving immutable quote snapshots with full app state (all 3 Zustand stores + calculated results + monthly P&L arrays) as JSONB columns.

Output: Migration 004, QuoteRepository, Pydantic schemas, DecimalEncoder, mock DB handlers, Wave 0 test stubs.
</objective>

<execution_context>
@/Users/abu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quote-persistence-and-history/04-CONTEXT.md
@.planning/phases/04-quote-persistence-and-history/04-RESEARCH.md
@.planning/phases/03-pricing-engine/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From fastapi-project/app/db/base_repository.py:
```python
class BaseRepository:
    def __init__(self, conn: asyncpg.Connection):
        self.conn = conn
    async def fetch_one(self, query: str, *args: Any) -> dict | None
    async def fetch_many(self, query: str, *args: Any) -> list[dict]
    async def execute(self, query: str, *args: Any) -> str
```

From fastapi-project/app/pricing/schemas.py:
```python
class ComponentBreakdown(BaseModel):
    aircraft_eur_per_bh: Decimal
    crew_eur_per_bh: Decimal
    maintenance_eur_per_bh: Decimal
    insurance_eur_per_bh: Decimal
    doc_eur_per_bh: Decimal
    other_cogs_eur_per_bh: Decimal
    overhead_eur_per_bh: Decimal
    total_cost_per_bh: Decimal
    revenue_per_bh: Decimal
    margin_percent: Decimal
    final_rate_per_bh: Decimal

class MsnPnlResult(BaseModel):
    msn: int
    aircraft_type: str
    breakdown: ComponentBreakdown
    monthly_cost: Decimal
    monthly_revenue: Decimal
    monthly_pnl: Decimal
```

Existing migration pattern (from 003_create_pricing_config.sql):
- Uses CREATE TABLE IF NOT EXISTS
- NUMERIC(12,2) for dollar amounts, NUMERIC(10,4) for rates
- Partial unique indexes for is_current flags
- REFERENCES users(id) for ownership

Existing mock DB pattern (from tests/conftest.py):
- MockConnection._detect_table() routes queries by FROM/INTO clause
- Table-specific handler methods return fixture data
- New tables must be added to _detect_table() routing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and Pydantic schemas</name>
  <files>
    fastapi-project/migrations/004_create_quotes.sql,
    fastapi-project/app/quotes/__init__.py,
    fastapi-project/app/quotes/schemas.py
  </files>
  <action>
    1. Create migration `004_create_quotes.sql` with three tables:

    **quote_sequences** — Atomic counter per client code:
    ```sql
    CREATE TABLE IF NOT EXISTS quote_sequences (
        client_code TEXT PRIMARY KEY,
        last_seq INTEGER NOT NULL DEFAULT 0
    );
    ```

    **quotes** — Hybrid normalized metadata + JSONB snapshots:
    - `id SERIAL PRIMARY KEY`
    - `quote_number TEXT NOT NULL UNIQUE` (e.g., "EZJ-001")
    - `client_name TEXT NOT NULL`
    - `client_code TEXT NOT NULL` (2-4 uppercase letters)
    - `status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'sent', 'accepted', 'rejected'))`
    - Quick-access metadata: `exchange_rate NUMERIC(8,4)`, `margin_percent NUMERIC(6,4)`, `total_eur_per_bh NUMERIC(12,4)`, `msn_list INTEGER[]`, `period_start TEXT`, `period_end TEXT`
    - JSONB snapshot columns: `pricing_config_snapshot JSONB NOT NULL`, `crew_config_snapshot JSONB NOT NULL`, `costs_config_snapshot JSONB NOT NULL`, `dashboard_state JSONB NOT NULL`
    - Ownership: `created_by INTEGER NOT NULL REFERENCES users(id)`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
    - Indexes: `idx_quotes_client(client_code, created_at DESC)`, `idx_quotes_status(status)`, `idx_quotes_created(created_at DESC)`, `idx_quotes_msn_list USING GIN(msn_list)`

    **quote_msn_snapshots** — Per-MSN data within a quote:
    - `id SERIAL PRIMARY KEY`
    - `quote_id INTEGER NOT NULL REFERENCES quotes(id) ON DELETE CASCADE`
    - `msn INTEGER NOT NULL`, `aircraft_type TEXT NOT NULL`, `aircraft_id INTEGER NOT NULL`
    - `msn_input JSONB NOT NULL` (full MsnInput object)
    - `breakdown JSONB NOT NULL` (ComponentBreakdown)
    - `monthly_pnl JSONB NOT NULL` (full monthly P&L array data)
    - `monthly_cost NUMERIC(12,2)`, `monthly_revenue NUMERIC(12,2)`
    - `UNIQUE(quote_id, msn)`
    - Index: `idx_quote_msn_snapshots_quote(quote_id)`

    2. Create `app/quotes/__init__.py` (empty).

    3. Create `app/quotes/schemas.py` with Pydantic models:
    - `SaveQuoteRequest`: client_name (str), client_code (str, 2-4 uppercase letters validated with field_validator), dashboard_state (dict), pricing_config_snapshot (dict), crew_config_snapshot (dict), costs_config_snapshot (dict), msn_snapshots (list of MsnSnapshotInput)
    - `MsnSnapshotInput`: msn (int), aircraft_type (str), aircraft_id (int), msn_input (dict), breakdown (dict), monthly_pnl (dict)
    - `QuoteListItem`: id (int), quote_number (str), client_name (str), client_code (str), status (str), exchange_rate (Decimal), margin_percent (Decimal), total_eur_per_bh (Decimal | None), msn_list (list[int]), created_at (str), created_by (int)
    - `QuoteDetailResponse`: extends QuoteListItem with pricing_config_snapshot (dict), crew_config_snapshot (dict), costs_config_snapshot (dict), dashboard_state (dict), msn_snapshots (list of MsnSnapshotResponse)
    - `MsnSnapshotResponse`: id (int), msn (int), aircraft_type (str), aircraft_id (int), msn_input (dict), breakdown (dict), monthly_pnl (dict), monthly_cost (Decimal | None), monthly_revenue (Decimal | None)
    - `UpdateQuoteStatusRequest`: status (str, validated to be one of draft/sent/accepted/rejected)
    - Import Decimal from decimal, use it for all monetary fields.
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Claud/fastapi-project && python -c "from app.quotes.schemas import SaveQuoteRequest, QuoteListItem, QuoteDetailResponse, UpdateQuoteStatusRequest; print('Schemas OK')"</automated>
  </verify>
  <done>Migration 004 SQL file exists with all 3 tables. Pydantic schemas import cleanly with proper Decimal types. Client code validation rejects invalid formats.</done>
</task>

<task type="auto">
  <name>Task 2: QuoteRepository, service, mock DB, and test stubs</name>
  <files>
    fastapi-project/app/quotes/repository.py,
    fastapi-project/app/quotes/service.py,
    fastapi-project/tests/conftest.py,
    fastapi-project/tests/test_quotes.py
  </files>
  <action>
    1. Create `app/quotes/service.py`:
    - `DecimalEncoder(json.JSONEncoder)`: converts Decimal to str in `default()` method. Import json and Decimal.
    - `assemble_snapshot_json(data: dict) -> str`: Convenience wrapper calling `json.dumps(data, cls=DecimalEncoder)`.
    - Keep this module pure (no DB, no I/O).

    2. Create `app/quotes/repository.py` with `QuoteRepository(BaseRepository)`:
    - `generate_quote_number(client_code: str) -> str`: Uses `INSERT INTO quote_sequences (client_code, last_seq) VALUES ($1, 1) ON CONFLICT (client_code) DO UPDATE SET last_seq = quote_sequences.last_seq + 1 RETURNING last_seq`. Format as `{client_code}-{seq:03d}`.
    - `create_quote(quote_number, client_name, client_code, created_by, exchange_rate, margin_percent, total_eur_per_bh, msn_list, period_start, period_end, pricing_config_snapshot, crew_config_snapshot, costs_config_snapshot, dashboard_state) -> dict`: INSERT with `$N::jsonb` casts for JSONB columns. Uses `assemble_snapshot_json()` from service.py for each JSONB field. Returns RETURNING *.
    - `create_msn_snapshot(quote_id, msn, aircraft_type, aircraft_id, msn_input, breakdown, monthly_pnl, monthly_cost, monthly_revenue) -> dict`: INSERT into quote_msn_snapshots with JSONB casts.
    - `list_quotes(search: str | None, status: str | None, limit: int, offset: int) -> list[dict]`: SELECT from quotes with optional WHERE on client_name ILIKE or quote_number ILIKE, optional status filter, ORDER BY created_at DESC, LIMIT/OFFSET.
    - `count_quotes(search: str | None, status: str | None) -> int`: COUNT query matching list_quotes filters.
    - `get_quote(quote_id: int) -> dict | None`: SELECT * FROM quotes WHERE id = $1.
    - `get_quote_msn_snapshots(quote_id: int) -> list[dict]`: SELECT * FROM quote_msn_snapshots WHERE quote_id = $1 ORDER BY msn.
    - `update_status(quote_id: int, status: str) -> dict | None`: UPDATE quotes SET status = $1 WHERE id = $2 RETURNING *. Only updates status column (snapshot columns remain immutable).

    3. Extend `tests/conftest.py` MockConnection:
    - Add `_handle_quotes()`, `_handle_quote_msn_snapshots()`, `_handle_quote_sequences()` methods
    - Update `_detect_table()` to route quote queries before pricing tables
    - Add `test_quote` fixture returning a sample quote dict with all required fields including JSONB snapshots as dicts
    - Add `test_quote_msn_snapshot` fixture with sample MSN snapshot data

    4. Create `tests/test_quotes.py` with Wave 0 test stubs:
    - `test_create_quote`: POST /quotes with valid payload returns 201 + quote_number
    - `test_quote_immutable`: Verify no UPDATE endpoint exists for snapshot columns
    - `test_list_quotes_filtered`: GET /quotes?search=EZJ returns filtered results
    - `test_get_quote_detail`: GET /quotes/{id} returns full quote with msn_snapshots
    - `test_update_status`: PATCH /quotes/{id}/status with valid status returns updated quote
    - `test_update_status_unauthorized`: Non-creator non-admin gets 403
    - `test_list_quotes_by_status`: GET /quotes?status=draft returns only draft quotes
    - All stubs should be marked with `@pytest.mark.asyncio` and use the async_client + _login pattern from existing tests.
    - Stubs should have the test body filled in (not just `pass`) so they fail meaningfully as RED tests.
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Claud/fastapi-project && python -c "from app.quotes.repository import QuoteRepository; from app.quotes.service import DecimalEncoder, assemble_snapshot_json; print('Repository + Service OK')" && python -m pytest tests/test_quotes.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>QuoteRepository extends BaseRepository with all CRUD methods. DecimalEncoder preserves Decimal as string in JSON. Mock DB handlers route quote queries correctly. Test stubs exist and fail (RED) because router is not yet registered — this is expected and will turn GREEN in Plan 03.</done>
</task>

</tasks>

<verification>
- `python -c "from app.quotes.schemas import SaveQuoteRequest, QuoteDetailResponse"` imports clean
- `python -c "from app.quotes.repository import QuoteRepository"` imports clean
- `python -c "from app.quotes.service import DecimalEncoder; import json; from decimal import Decimal; assert json.dumps({'rate': Decimal('185000.00')}, cls=DecimalEncoder) == '{\"rate\": \"185000.00\"}'"`
- Existing test suite still passes: `cd /Users/abu/Documents/Claud/fastapi-project && python -m pytest tests/ -x -q --ignore=tests/test_quotes.py`
</verification>

<success_criteria>
- Migration 004 DDL creates quotes, quote_msn_snapshots, and quote_sequences tables with correct types and constraints
- QuoteRepository.generate_quote_number produces "EZJ-001" format with atomic counter
- DecimalEncoder converts Decimal("185000.00") to "185000.00" (string, not float)
- All existing tests remain green (no regression)
- Wave 0 test stubs exist and fail meaningfully (RED -- router not yet registered)
</success_criteria>

<output>
After completion, create `.planning/phases/04-quote-persistence-and-history/04-01-SUMMARY.md`
</output>
