---
phase: 03-pricing-engine
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - nextjs-project/src/app/(dashboard)/pnl/page.tsx
  - nextjs-project/src/components/pricing/PnlTable.tsx
  - nextjs-project/src/components/pricing/MsnSwitcher.tsx
  - nextjs-project/src/components/pricing/MarginInput.tsx
  - nextjs-project/src/app/(dashboard)/crew/page.tsx
  - nextjs-project/src/components/crew/CrewConfigTable.tsx
  - nextjs-project/src/app/(dashboard)/pricing/page.tsx
autonomous: false
requirements: [PRIC-02, PRIC-03, PRIC-04, CONF-01]

must_haves:
  truths:
    - "P&L page displays per-BH cost breakdown for all 7 components in a table matching Excel UNA Project by AC structure"
    - "User can switch between individual MSN P&L and total project P&L"
    - "User can enter margin percentage and see final EUR/BH rate update in real-time"
    - "Crew page shows all crew cost parameters for A320 and A321"
    - "Admin can edit crew cost parameters through the Crew page UI"
    - "Old /pricing route redirects to /pnl"
  artifacts:
    - path: "nextjs-project/src/app/(dashboard)/pnl/page.tsx"
      provides: "P&L page with full financial statement view"
      min_lines: 20
    - path: "nextjs-project/src/components/pricing/PnlTable.tsx"
      provides: "P&L table component replicating UNA Project by AC Excel structure"
      min_lines: 100
    - path: "nextjs-project/src/components/pricing/MsnSwitcher.tsx"
      provides: "MSN selector with Total Project option"
      min_lines: 20
    - path: "nextjs-project/src/components/pricing/MarginInput.tsx"
      provides: "Margin percentage input with live final rate display"
      min_lines: 20
    - path: "nextjs-project/src/app/(dashboard)/crew/page.tsx"
      provides: "Crew cost assumptions page"
      min_lines: 20
    - path: "nextjs-project/src/components/crew/CrewConfigTable.tsx"
      provides: "Admin-editable crew configuration table for A320 and A321"
      min_lines: 60
  key_links:
    - from: "nextjs-project/src/components/pricing/PnlTable.tsx"
      to: "nextjs-project/src/stores/pricing-store.ts"
      via: "usePricingStore for results"
      pattern: "usePricingStore"
    - from: "nextjs-project/src/components/pricing/MsnSwitcher.tsx"
      to: "nextjs-project/src/stores/pricing-store.ts"
      via: "selectedMsn state"
      pattern: "setSelectedMsn"
    - from: "nextjs-project/src/components/crew/CrewConfigTable.tsx"
      to: "nextjs-project/src/app/actions/pricing.ts"
      via: "updateCrewConfigAction Server Action"
      pattern: "updateCrewConfigAction"
---

<objective>
Build the P&L statement page (replicating Excel's "UNA Project by AC" sheet), the Crew cost assumptions page, and connect everything with MSN switching and margin controls.

Purpose: The P&L page is the core output of the pricing engine -- it shows users the full financial statement per MSN or for the total project. The Crew page enables admin editing of crew cost parameters. Together these complete the pricing workflow.
Output: P&L page with component breakdown table, MSN switcher, margin input. Crew page with editable parameters. Human verification checkpoint.
</objective>

<execution_context>
@/Users/abu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pricing-engine/03-CONTEXT.md
@.planning/phases/03-pricing-engine/03-RESEARCH.md
@.planning/phases/03-pricing-engine/03-01-SUMMARY.md
@.planning/phases/03-pricing-engine/03-04-SUMMARY.md

<interfaces>
<!-- From Plan 04 (store + actions) -->

From nextjs-project/src/stores/pricing-store.ts:
```typescript
interface PricingStore {
  exchangeRate: string
  marginPercent: string
  msnInputs: MsnInput[]
  selectedMsn: number | null     // null = total project
  msnResults: MsnPnlResult[]
  totalResult: ComponentBreakdown | null
  isCalculating: boolean
  setSelectedMsn: (msn: number | null) => void
  setMarginPercent: (margin: string) => void
  setResults: (msnResults: MsnPnlResult[], total: ComponentBreakdown | null) => void
}
```

From nextjs-project/src/app/actions/pricing.ts:
```typescript
export async function calculatePnlAction(inputs): Promise<CalculateResponse | {error: string}>
export async function fetchCrewConfigAction(): Promise<CrewConfig[] | {error: string}>
export async function updateCrewConfigAction(formData): Promise<CrewConfig | {error: string}>
```

P&L table structure (from Excel "UNA Project by AC" sheet):
```
Revenue
  Block Hours Rate (EUR/BH)     [final_rate_per_bh]
  Monthly Block Hours            [mgh]
  Monthly Revenue                [monthly_revenue]

Cost of Revenue
  A - Aircraft                   [aircraft_eur_per_bh]  [monthly = rate * mgh]
  C - Crew                       [crew_eur_per_bh]       [monthly]
  M - Maintenance                [maintenance_eur_per_bh] [monthly]
  I - Insurance                  [insurance_eur_per_bh]   [monthly]
  DOC - Direct Operating Costs   [doc_eur_per_bh]         [monthly]
  Other COGS                     [other_cogs_eur_per_bh]  [monthly]
  Total Cost of Revenue          [sum of above]           [monthly]

Gross Profit                     [revenue - cost]         [monthly]

Overhead                         [overhead_eur_per_bh]    [monthly]

Net Operating Income             [gross_profit - overhead] [monthly]

Margin                           [margin_percent]%
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: P&L page with table, MSN switcher, and margin input</name>
  <files>
    nextjs-project/src/app/(dashboard)/pnl/page.tsx,
    nextjs-project/src/components/pricing/PnlTable.tsx,
    nextjs-project/src/components/pricing/MsnSwitcher.tsx,
    nextjs-project/src/components/pricing/MarginInput.tsx,
    nextjs-project/src/app/(dashboard)/pricing/page.tsx
  </files>
  <action>
**nextjs-project/src/app/(dashboard)/pnl/page.tsx**:
- Server Component (minimal) that renders the client-side P&L view
- No data fetching needed here -- data comes from Zustand store (populated by Dashboard)
- Renders MsnSwitcher + MarginInput + PnlTable in a column layout
- If no MSN inputs yet, show a message directing user to Dashboard to configure inputs

**nextjs-project/src/components/pricing/MsnSwitcher.tsx** ('use client'):
- Reads msnInputs and selectedMsn from usePricingStore
- Renders a row of toggle buttons: "Total Project" + one button per MSN (showing MSN number and registration)
- Active button highlighted with indigo-400 bg
- Clicking sets selectedMsn (null for total, or specific MSN number)
- If only one MSN, still show Total and the single MSN option

**nextjs-project/src/components/pricing/MarginInput.tsx** ('use client'):
- Reads marginPercent from usePricingStore
- Renders a labeled number input for margin % (step="0.01", min="0", max="99")
- On change, calls setMarginPercent which triggers recalculation (via Dashboard's debounced effect, or via its own effect that calls calculatePnlAction)
- Displays the resulting final EUR/BH rate prominently next to the input (from store's current result)
- Format: "Margin: [input]% => Final Rate: EUR [value]/BH"

**nextjs-project/src/components/pricing/PnlTable.tsx** ('use client'):
- Reads selectedMsn, msnResults, totalResult from usePricingStore
- Determines which data to show: if selectedMsn is null, use totalResult. Otherwise find the matching MsnPnlResult.
- Renders a structured financial table replicating the Excel "UNA Project by AC" sheet:

Table structure (two columns: EUR/BH rate | Monthly EUR):
```
REVENUE
  Block Hours Rate         | [final_rate_per_bh]      | --
  Monthly Block Hours      | [mgh]                    | --
  Monthly Revenue          | --                       | [monthly_revenue]

COST OF REVENUE
  A - Aircraft             | [aircraft_eur_per_bh]    | [rate * mgh]
  C - Crew                 | [crew_eur_per_bh]        | [rate * mgh]
  M - Maintenance          | [maintenance_eur_per_bh] | [rate * mgh]
  I - Insurance            | [insurance_eur_per_bh]   | [rate * mgh]
  DOC                      | [doc_eur_per_bh]         | [rate * mgh]
  Other COGS               | [other_cogs_eur_per_bh]  | [rate * mgh]
  ─────────────────────────────────────────────────────
  Total Cost of Revenue    | [total_cost_per_bh]      | [total * mgh]

GROSS PROFIT               | [revenue - total_cost]   | [monthly difference]

OVERHEAD
  Overhead                 | [overhead_eur_per_bh]    | [rate * mgh]

NET OPERATING INCOME       | [gross - overhead]       | [monthly]
```

- Styling: AeroVista dark theme table. bg-gray-900 background, gray-800 borders, gray-100 text. Section headers (REVENUE, COST OF REVENUE, etc.) in uppercase text-xs text-gray-500. Number values right-aligned with monospace font. Negative P&L values in red-400. Positive in green-400. Total rows with font-semibold and top border.
- Numbers formatted with 2 decimal places and EUR symbol. Monthly values with thousands separator.
- Loading state: if isCalculating, show subtle opacity/skeleton on the numbers
- Empty state: if no results yet, show "Configure MSNs on the Dashboard to see P&L calculations"

**nextjs-project/src/app/(dashboard)/pricing/page.tsx**:
- Replace the placeholder with a redirect to /pnl:
  ```typescript
  import { redirect } from 'next/navigation'
  export default function PricingPage() { redirect('/pnl') }
  ```
  This ensures any old /pricing links redirect to the new P&L page.
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Claud/nextjs-project && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>P&L page renders structured financial table from Zustand store data. MSN switcher toggles between individual and total views. Margin input updates final rate. Old /pricing redirects to /pnl. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Crew cost assumptions page with admin editing</name>
  <files>
    nextjs-project/src/app/(dashboard)/crew/page.tsx,
    nextjs-project/src/components/crew/CrewConfigTable.tsx
  </files>
  <action>
**nextjs-project/src/app/(dashboard)/crew/page.tsx**:
- Server Component that fetches crew config (fetchCrewConfigAction) and isAdmin from /auth/me
- Passes crew config data and isAdmin to CrewConfigTable client component
- Uses cookies() forwarding pattern from aircraft page

**nextjs-project/src/components/crew/CrewConfigTable.tsx** ('use client'):
- Receives crew config data (A320 + A321) and isAdmin prop
- Displays two sections (one per aircraft type) or a tabbed/side-by-side layout

Each section shows a table with all crew cost parameters:
| Parameter | A320 Value | A321 Value |
Or two separate cards, one for A320 and one for A321.

Parameters to display:
- Pilot Salary (monthly EUR)
- Senior Attendant Salary (monthly EUR)
- Regular Attendant Salary (monthly EUR)
- Per Diem Rate (EUR/day)
- Accommodation Budget (monthly EUR)
- Training Budget (total EUR)
- Uniform Budget (total EUR)

Also show crew composition info (read-only) for reference:
- Wet lease: A320 = 2P + 1S + 3R, A321 = 2P + 1S + 4R
- Damp lease: 2P only
- Moist lease: 2P + 1S

Admin edit mode:
- If isAdmin, show "Edit" button per aircraft type section
- On click, toggle to edit mode (same pattern as aircraft rates editing)
- Each value becomes an editable number input
- "Save" button calls updateCrewConfigAction via Server Action
- On success, revalidate and show updated values
- On error, show error message
- "Cancel" button reverts to view mode

Non-admin view: Values displayed as read-only text. No edit button.

Styling: AeroVista dark theme. Cards with bg-gray-900, borders gray-800, labels gray-400, values gray-100. Edit inputs match existing aircraft page style. Section headers with aircraft type icon.
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Claud/nextjs-project && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Crew page shows all crew cost parameters for A320 and A321. Admin can edit and save crew config (creates new version via append-only pattern). Non-admin sees read-only view. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete pricing engine</name>
  <files>none</files>
  <action>
Human verifies the complete pricing engine end-to-end. All automated work is complete:
1. Database schema with versioned pricing_config, crew_config tables and project/MSN tables
2. Pure Python calculation engine for all 7 ACMI components with Decimal precision
3. Backend API: calculate endpoint, config CRUD, crew config CRUD, project management
4. Frontend Dashboard with per-MSN operational inputs and exchange rate control
5. P&L page with full financial statement table, MSN switcher, and margin input
6. Crew page with admin-editable crew cost parameters
7. Sidebar updated: "P&L" (renamed from Pricing), "Crew" (new)

Verification steps:
1. Start backend: `cd fastapi-project && uvicorn app.main:app --reload`
2. Start frontend: `cd nextjs-project && npm run dev`
3. Log in as admin user
4. Dashboard: navigate, add MSN, enter inputs (MGH=350, CR=1.0, benign, 12mo, wet), change exchange rate, verify calculation triggers
5. P&L: navigate, verify 7-component breakdown table, toggle MSN switcher, enter margin %, verify final rate updates
6. Crew: navigate, verify A320/A321 parameters, edit a value as admin, verify save works
7. Sidebar: confirm shows Dashboard, P&L, Crew, Quotes, Aircraft, Admin; confirm /pricing redirects to /pnl
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Claud && python3 -m pytest fastapi-project/tests/ -x</automated>
  </verify>
  <done>Human confirms: Dashboard inputs work, P&L table shows correct breakdown, margin updates final rate, crew config is editable by admin, sidebar navigation is correct.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd nextjs-project && npx tsc --noEmit`
- Backend tests pass: `python3 -m pytest fastapi-project/tests/ -x`
- P&L page accessible at /pnl
- Crew page accessible at /crew
- Old /pricing redirects to /pnl
</verification>

<success_criteria>
- P&L table replicates Excel "UNA Project by AC" structure with Revenue, Cost of Revenue, Gross Profit, Overhead, Net Operating Income sections
- MSN switcher toggles between per-MSN and total project P&L views
- Margin input produces correct final rate and updates table in real-time
- Crew page displays A320 and A321 parameters, admin can edit them
- All pricing calculations use server-side Decimal (no JS float math)
- Human verifies end-to-end flow works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-pricing-engine/03-05-SUMMARY.md`
</output>
