---
phase: 03-pricing-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fastapi-project/migrations/003_create_pricing_config.sql
  - fastapi-project/app/pricing/__init__.py
  - fastapi-project/app/pricing/schemas.py
  - fastapi-project/app/pricing/repository.py
  - fastapi-project/scripts/seed_pricing_config.py
  - fastapi-project/tests/conftest.py
autonomous: true
requirements: [PRIC-06, CONF-02, CONF-03]

must_haves:
  truths:
    - "Pricing config and crew config tables exist with versioning (append-only)"
    - "Pricing repositories return Decimal values for all monetary fields"
    - "Seed data from Excel C and M/I/Overhead sheets populates initial config"
    - "Existing test suite passes with no regressions after new mock DB handlers added"
  artifacts:
    - path: "fastapi-project/migrations/003_create_pricing_config.sql"
      provides: "DDL for pricing_config, crew_config, pricing_projects, project_msn_inputs tables"
      contains: "CREATE TABLE pricing_config"
    - path: "fastapi-project/app/pricing/schemas.py"
      provides: "Pydantic models for pricing inputs, config, calculation output with Decimal types"
      min_lines: 80
    - path: "fastapi-project/app/pricing/repository.py"
      provides: "PricingConfigRepository and CrewConfigRepository with get_current, create_version, get_version"
      exports: ["PricingConfigRepository", "CrewConfigRepository", "ProjectRepository"]
    - path: "fastapi-project/scripts/seed_pricing_config.py"
      provides: "Seed script loading config values from Excel workbook"
      min_lines: 40
  key_links:
    - from: "fastapi-project/app/pricing/repository.py"
      to: "fastapi-project/app/db/base_repository.py"
      via: "class inheritance"
      pattern: "class.*Repository\\(BaseRepository\\)"
    - from: "fastapi-project/app/pricing/schemas.py"
      to: "decimal.Decimal"
      via: "Pydantic Decimal fields"
      pattern: "Decimal"
---

<objective>
Database foundation for the pricing engine: versioned config tables, project/MSN data model, repositories, Pydantic schemas, seed data, and mock DB handlers.

Purpose: Every subsequent pricing plan depends on these tables, schemas, and repositories. This is the data layer that enables calculation, API, and frontend work.
Output: Migration SQL, repository classes, Pydantic schemas, seed script, extended mock DB in conftest.py.
</objective>

<execution_context>
@/Users/abu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pricing-engine/03-CONTEXT.md
@.planning/phases/03-pricing-engine/03-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From fastapi-project/app/db/base_repository.py:
```python
class BaseRepository:
    def __init__(self, conn: asyncpg.Connection):
        self.conn = conn
    async def fetch_one(self, query: str, *args) -> dict | None
    async def fetch_many(self, query: str, *args) -> list[dict]
    async def execute(self, query: str, *args) -> str
```

From fastapi-project/app/aircraft/schemas.py (Decimal pattern):
```python
from decimal import Decimal
from pydantic import BaseModel

class AircraftDetailResponse(BaseModel):
    lease_rent_usd: Decimal | None = None
    # ... all monetary fields typed as Decimal
```

From fastapi-project/migrations/002_create_aircraft.sql (NUMERIC conventions):
```sql
-- NUMERIC(12,2) for dollar amounts
-- NUMERIC(10,4) for rates
-- NUMERIC(6,4) for escalation/percentages
-- NUMERIC(8,4) for exchange rate
-- NUMERIC(4,1) for fleet size
-- NUMERIC(8,2) for block hours
```

From fastapi-project/tests/conftest.py (mock DB pattern):
```python
class MockConnection:
    def __init__(self, store: dict):
        self.store = store
    # Table-specific handlers: _handle_{table}_select, _handle_{table}_insert, _handle_{table}_update
    # _detect_table() routes queries to correct handler
```

From fastapi-project/app/aircraft/repository.py (repository pattern):
```python
class AircraftRepository(BaseRepository):
    async def list_aircraft(self, search: str | None = None) -> list[dict]
    async def fetch_by_msn(self, msn: int) -> dict | None
    async def update_rates(self, aircraft_id: int, **fields) -> dict | None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL and seed script for pricing config tables</name>
  <files>
    fastapi-project/migrations/003_create_pricing_config.sql,
    fastapi-project/scripts/seed_pricing_config.py
  </files>
  <action>
Create migration 003_create_pricing_config.sql with four tables:

1. **pricing_config** (versioned, append-only):
   - id SERIAL PRIMARY KEY
   - version INTEGER NOT NULL DEFAULT 1
   - exchange_rate NUMERIC(8,4) NOT NULL DEFAULT 0.85
   - insurance_usd NUMERIC(12,2) NOT NULL
   - doc_total_budget NUMERIC(12,2) NOT NULL
   - overhead_total_budget NUMERIC(12,2) NOT NULL
   - other_cogs_monthly NUMERIC(12,2) NOT NULL
   - line_maintenance_monthly NUMERIC(12,2) NOT NULL
   - base_maintenance_monthly NUMERIC(12,2) NOT NULL
   - personnel_salary_monthly NUMERIC(12,2) NOT NULL
   - c_check_monthly NUMERIC(12,2) NOT NULL
   - maintenance_training_monthly NUMERIC(12,2) NOT NULL
   - spare_parts_rate NUMERIC(10,4) NOT NULL
   - maintenance_per_diem NUMERIC(12,2) NOT NULL
   - average_active_fleet NUMERIC(4,1) NOT NULL DEFAULT 11.0
   - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - created_by INTEGER REFERENCES users(id)
   - is_current BOOLEAN NOT NULL DEFAULT TRUE
   - CREATE UNIQUE INDEX idx_pricing_config_current ON pricing_config (is_current) WHERE is_current = TRUE

2. **crew_config** (versioned, append-only, per aircraft_type):
   - id SERIAL PRIMARY KEY
   - version INTEGER NOT NULL DEFAULT 1
   - aircraft_type TEXT NOT NULL DEFAULT 'A320'
   - pilot_salary_monthly NUMERIC(12,2) NOT NULL
   - senior_attendant_salary_monthly NUMERIC(12,2) NOT NULL
   - regular_attendant_salary_monthly NUMERIC(12,2) NOT NULL
   - per_diem_rate NUMERIC(10,2) NOT NULL
   - accommodation_monthly_budget NUMERIC(12,2) NOT NULL
   - training_total_budget NUMERIC(12,2) NOT NULL
   - uniform_total_budget NUMERIC(12,2) NOT NULL
   - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - created_by INTEGER REFERENCES users(id)
   - is_current BOOLEAN NOT NULL DEFAULT TRUE
   - CREATE UNIQUE INDEX idx_crew_config_current ON crew_config (aircraft_type, is_current) WHERE is_current = TRUE

3. **pricing_projects** (mutable, session-based):
   - id SERIAL PRIMARY KEY
   - name TEXT
   - exchange_rate NUMERIC(8,4) NOT NULL DEFAULT 0.85
   - margin_percent NUMERIC(6,4) DEFAULT 0
   - config_version_id INTEGER REFERENCES pricing_config(id)
   - crew_config_a320_id INTEGER REFERENCES crew_config(id)
   - crew_config_a321_id INTEGER REFERENCES crew_config(id)
   - created_by INTEGER REFERENCES users(id)
   - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()

4. **project_msn_inputs** (mutable, per-MSN within a project):
   - id SERIAL PRIMARY KEY
   - project_id INTEGER NOT NULL REFERENCES pricing_projects(id) ON DELETE CASCADE
   - aircraft_id INTEGER NOT NULL REFERENCES aircraft(id)
   - mgh NUMERIC(8,2) NOT NULL (Monthly Guaranteed Hours)
   - cycle_ratio NUMERIC(6,4) NOT NULL
   - environment TEXT NOT NULL CHECK (environment IN ('benign', 'hot'))
   - period_months INTEGER NOT NULL DEFAULT 12
   - lease_type TEXT NOT NULL DEFAULT 'wet' CHECK (lease_type IN ('wet', 'damp', 'moist'))
   - crew_sets INTEGER NOT NULL DEFAULT 4
   - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - UNIQUE(project_id, aircraft_id)

Create seed_pricing_config.py following the established pattern from seed_aircraft.py:
- Use openpyxl to read "UNA Pricing Model 1 year.xlsx"
- Extract crew config values from C sheet (A320 and A321 rows)
- Extract maintenance/insurance/DOC/overhead/other COGS values from M/I/Overhead sheet
- Extract exchange rate from Summary sheet
- Hardcode all values from Excel audit for reliability (like seed_aircraft.py)
- Optional Excel validation for mismatch detection
- Support --dry-run flag
- INSERT initial version 1 rows for pricing_config and crew_config (A320 + A321)
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('fastapi-project/scripts/seed_pricing_config.py').read()); print('syntax ok')" && test -f fastapi-project/migrations/003_create_pricing_config.sql && SQL_FILE=fastapi-project/migrations/003_create_pricing_config.sql && CREATE_COUNT=$(grep -c 'CREATE TABLE' "$SQL_FILE") && test "$CREATE_COUNT" -eq 4 && echo "4 CREATE TABLE statements found" && ! grep -qi 'FLOAT\|DOUBLE PRECISION\|REAL' "$SQL_FILE" && echo "no FLOAT/DOUBLE/REAL types found — all NUMERIC"</automated>
  </verify>
  <done>Migration SQL creates 4 tables with correct NUMERIC types and constraints. No FLOAT or DOUBLE PRECISION types present. Seed script hardcodes Excel-audited values and can INSERT initial config versions.</done>
</task>

<task type="auto">
  <name>Task 2: Pydantic schemas, repositories, and mock DB handlers</name>
  <files>
    fastapi-project/app/pricing/__init__.py,
    fastapi-project/app/pricing/schemas.py,
    fastapi-project/app/pricing/repository.py,
    fastapi-project/tests/conftest.py
  </files>
  <action>
Create fastapi-project/app/pricing/__init__.py (empty).

Create fastapi-project/app/pricing/schemas.py with Pydantic models:
- **PricingInputs**: msn (int), mgh (Decimal), cycle_ratio (Decimal), environment (str), period_months (int), lease_type (str), crew_sets (int)
- **CalculateRequest**: exchange_rate (Decimal), margin_percent (Decimal), msn_inputs (list[PricingInputs])
- **ComponentBreakdown**: aircraft_eur_per_bh, crew_eur_per_bh, maintenance_eur_per_bh, insurance_eur_per_bh, doc_eur_per_bh, other_cogs_eur_per_bh, overhead_eur_per_bh, total_cost_per_bh, revenue_per_bh, margin_percent, final_rate_per_bh -- all Decimal
- **MsnPnlResult**: msn (int), aircraft_type (str), breakdown (ComponentBreakdown), monthly_cost (Decimal), monthly_revenue (Decimal), monthly_pnl (Decimal)
- **CalculateResponse**: msn_results (list[MsnPnlResult]), total (ComponentBreakdown | None)
- **PricingConfigResponse**: all fields from pricing_config table mapped to Decimal types
- **CrewConfigResponse**: all fields from crew_config table mapped to Decimal types
- **UpdatePricingConfigRequest**: all editable fields as Optional[Decimal]
- **UpdateCrewConfigRequest**: all editable crew fields as Optional[Decimal], aircraft_type required
- **ProjectResponse**: id, name, exchange_rate, margin_percent, config_version_id, msn_inputs list
- **CreateProjectRequest**: name (Optional[str])
- **AddMsnInputRequest**: aircraft_id (int), mgh (Decimal), cycle_ratio (Decimal), environment (str), period_months (int), lease_type (str), crew_sets (int)
All monetary fields must be Decimal type, never float.

Create fastapi-project/app/pricing/repository.py with three repository classes extending BaseRepository:
- **PricingConfigRepository**:
  - get_current() -> dict | None: SELECT * FROM pricing_config WHERE is_current = TRUE
  - get_version(version_id: int) -> dict | None: SELECT by id
  - create_version(created_by: int, **fields) -> dict: Get current, UPDATE is_current=FALSE on old, INSERT new row with version+1 and is_current=TRUE
- **CrewConfigRepository**:
  - get_current(aircraft_type: str) -> dict | None: SELECT WHERE aircraft_type=$1 AND is_current=TRUE
  - get_version(version_id: int) -> dict | None: SELECT by id
  - get_all_current() -> list[dict]: SELECT WHERE is_current=TRUE (both A320+A321)
  - create_version(aircraft_type: str, created_by: int, **fields) -> dict: Same append-only pattern
- **ProjectRepository**:
  - create_project(created_by: int, name: str | None = None) -> dict
  - get_project(project_id: int) -> dict | None
  - list_projects(user_id: int) -> list[dict]
  - update_project(project_id: int, **fields) -> dict | None
  - add_msn_input(project_id: int, **fields) -> dict
  - update_msn_input(input_id: int, **fields) -> dict | None
  - get_msn_inputs(project_id: int) -> list[dict]
  - delete_msn_input(input_id: int) -> None

Extend conftest.py MockConnection:
- Add "pricing_config", "crew_config", "pricing_projects", "project_msn_inputs" to db_store fixture
- Update _detect_table() to route these 4 new tables
- Add handlers: _handle_pricing_config_select, _handle_pricing_config_insert, _handle_pricing_config_update
- Add handlers: _handle_crew_config_select, _handle_crew_config_insert, _handle_crew_config_update
- Add handlers: _handle_projects_select, _handle_projects_insert, _handle_projects_update
- Add handlers: _handle_msn_inputs_select, _handle_msn_inputs_insert, _handle_msn_inputs_update, _handle_msn_inputs_delete
- Follow exact patterns from existing aircraft handlers (MockRecord, ON CONFLICT, RETURNING, SET clause parsing)
- Add test_pricing_config fixture that inserts initial config version 1 with realistic values
- Add test_crew_config fixture that inserts A320 and A321 crew configs

IMPORTANT: The is_current flag logic must work correctly in mock handlers -- when creating a new version, the handler must set is_current=FALSE on the previous row before inserting the new one.
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Claud && python3 -m pytest fastapi-project/tests/ -x --co -q 2>&1 | tail -5</automated>
  </verify>
  <done>Schemas import cleanly with all Decimal types. Repositories extend BaseRepository. Mock DB handles all 4 new tables. Existing test suite still collects and passes (no regressions). test_pricing_config and test_crew_config fixtures available.</done>
</task>

</tasks>

<verification>
- Migration SQL parseable and contains all 4 CREATE TABLE statements
- All monetary columns use NUMERIC (no FLOAT, no DOUBLE PRECISION)
- Schemas import: `python3 -c "from app.pricing.schemas import CalculateRequest, ComponentBreakdown"`
- Repos import: `python3 -c "from app.pricing.repository import PricingConfigRepository, CrewConfigRepository, ProjectRepository"`
- Existing tests pass: `python3 -m pytest fastapi-project/tests/ -x`
</verification>

<success_criteria>
- 4 new tables defined in migration with correct NUMERIC precision
- pricing_config and crew_config have append-only versioning (is_current flag + partial unique index)
- Pydantic schemas use Decimal for all monetary/rate fields
- Repositories follow BaseRepository pattern with get_current/create_version/get_version methods
- Mock DB handlers support all 4 new tables with no regression on existing tests
- Seed script hardcodes values from Excel audit
</success_criteria>

<output>
After completion, create `.planning/phases/03-pricing-engine/03-01-SUMMARY.md`
</output>
