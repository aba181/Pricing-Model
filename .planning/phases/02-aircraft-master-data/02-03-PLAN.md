---
phase: 02-aircraft-master-data
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - nextjs-project/src/app/(dashboard)/aircraft/page.tsx
  - nextjs-project/src/app/(dashboard)/aircraft/[msn]/page.tsx
  - nextjs-project/src/components/aircraft/AircraftTable.tsx
  - nextjs-project/src/components/aircraft/AircraftDetail.tsx
  - nextjs-project/src/components/aircraft/RatesSection.tsx
  - nextjs-project/src/components/aircraft/EprMatrixTable.tsx
  - nextjs-project/src/app/actions/aircraft.ts
autonomous: false
requirements: [ACFT-02, ACFT-03, ACFT-04]

must_haves:
  truths:
    - "User can see all aircraft in a table with MSN, type, registration, and fixed rates in USD and EUR"
    - "User can search the aircraft list by typing MSN or registration and results filter immediately"
    - "User can click an aircraft row and see all cost data: fixed rates, variable rates, escalation rates, and EPR matrix"
    - "Admin can edit cost parameters on the detail page and see changes immediately"
  artifacts:
    - path: "nextjs-project/src/app/(dashboard)/aircraft/page.tsx"
      provides: "Server Component fetching aircraft list from API"
      contains: "getAircraft"
    - path: "nextjs-project/src/app/(dashboard)/aircraft/[msn]/page.tsx"
      provides: "Server Component fetching aircraft detail by MSN"
      contains: "getAircraftDetail"
    - path: "nextjs-project/src/components/aircraft/AircraftTable.tsx"
      provides: "Client component with searchable, clickable aircraft table"
      contains: "use client"
    - path: "nextjs-project/src/components/aircraft/AircraftDetail.tsx"
      provides: "Client component orchestrating detail sections"
      contains: "use client"
    - path: "nextjs-project/src/components/aircraft/RatesSection.tsx"
      provides: "Rates display with inline edit for admin"
      contains: "use client"
    - path: "nextjs-project/src/components/aircraft/EprMatrixTable.tsx"
      provides: "EPR matrix table display"
      contains: "EprMatrixTable"
    - path: "nextjs-project/src/app/actions/aircraft.ts"
      provides: "Server Actions for rate updates"
      contains: "use server"
  key_links:
    - from: "nextjs-project/src/app/(dashboard)/aircraft/page.tsx"
      to: "/aircraft"
      via: "Server-side fetch with cookie forwarding"
      pattern: "fetch.*API_URL.*/aircraft"
    - from: "nextjs-project/src/app/(dashboard)/aircraft/[msn]/page.tsx"
      to: "/aircraft/{msn}"
      via: "Server-side fetch with cookie forwarding"
      pattern: "fetch.*API_URL.*/aircraft/"
    - from: "nextjs-project/src/app/actions/aircraft.ts"
      to: "/aircraft/{msn}/rates"
      via: "Server Action PUT request"
      pattern: "fetch.*aircraft.*rates"
    - from: "nextjs-project/src/components/aircraft/AircraftTable.tsx"
      to: "nextjs-project/src/app/(dashboard)/aircraft/[msn]/page.tsx"
      via: "Next.js Link or router.push to /aircraft/{msn}"
      pattern: "href.*aircraft|push.*aircraft"
---

<objective>
Build the aircraft frontend: searchable list page replacing the placeholder, detail page with all cost data sections, and admin edit capability via Server Actions.

Purpose: Delivers the user-facing aircraft browsing and admin editing experience. Replaces the Phase 2 placeholder page with real functionality.
Output: Aircraft list page with search, detail page with 4 sections (fixed rates, variable rates, escalation, EPR matrix), Server Actions for admin updates
</objective>

<execution_context>
@/Users/abu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-aircraft-master-data/02-CONTEXT.md
@.planning/phases/02-aircraft-master-data/02-RESEARCH.md
@.planning/phases/02-aircraft-master-data/02-02-SUMMARY.md

<interfaces>
<!-- API contracts from Plan 02 that this plan consumes -->

GET /aircraft (list endpoint):
```json
[
  {
    "id": 1,
    "msn": 3055,
    "aircraft_type": "A320",
    "registration": null,
    "lease_rent_usd": "185000.00",
    "six_year_check_usd": "14334.75",
    "twelve_year_check_usd": "7513.07",
    "ldg_usd": "4864.50",
    "lease_rent_eur": "157250.00",
    "six_year_check_eur": "12184.54",
    "twelve_year_check_eur": "6386.11",
    "ldg_eur": "4134.83"
  }
]
```

GET /aircraft/{msn} (detail endpoint):
```json
{
  "id": 1,
  "msn": 3055,
  "aircraft_type": "A320",
  "registration": null,
  "lease_rent_usd": "185000.00",
  "six_year_check_usd": "14334.75",
  "twelve_year_check_usd": "7513.07",
  "ldg_usd": "4864.50",
  "apu_rate_usd": "53.8200",
  "llp1_rate_usd": "341.7750",
  "llp2_rate_usd": "353.1350",
  "epr_escalation": "0.0500",
  "llp_escalation": "0.0800",
  "af_apu_escalation": "0.0300",
  "lease_rent_eur": "157250.00",
  "six_year_check_eur": "12184.54",
  "twelve_year_check_eur": "6386.11",
  "ldg_eur": "4134.83",
  "apu_rate_eur": "45.7470",
  "llp1_rate_eur": "290.5088",
  "llp2_rate_eur": "300.1648",
  "epr_matrix": [
    {"cycle_ratio": "1.0000", "benign_rate": "448.22", "hot_rate": "672.33"},
    {"cycle_ratio": "1.5000", "benign_rate": "319.07", "hot_rate": "478.61"}
  ]
}
```

PUT /aircraft/{msn}/rates (update endpoint, admin only):
Request: `{"lease_rent_usd": 200000}` (any subset of rate fields)
Response: Full detail response with updated values

From nextjs-project/src/app/(dashboard)/layout.tsx:
```tsx
// Dashboard layout wraps all (dashboard) pages with Sidebar + TopBar
// Children render in <main className="flex-1 overflow-auto p-6">
```

From nextjs-project/src/app/actions/auth.ts (Server Action pattern):
```tsx
'use server'
import { cookies } from 'next/headers'
const API_URL = process.env.API_URL ?? 'http://localhost:8000'
// Server Actions read cookie from cookies() and forward in fetch headers
```

From nextjs-project/src/lib/api.ts (client-side fetch):
```tsx
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>
// Uses NEXT_PUBLIC_API_URL, includes credentials for cookie
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aircraft list page with searchable table</name>
  <files>
    nextjs-project/src/app/(dashboard)/aircraft/page.tsx,
    nextjs-project/src/components/aircraft/AircraftTable.tsx
  </files>
  <action>
    Replace `nextjs-project/src/app/(dashboard)/aircraft/page.tsx` (currently a PlaceholderPage) with a Server Component that fetches aircraft data:

    1. Server Component structure:
       - Read `access_token` cookie via `cookies()` from `next/headers`
       - Fetch `${API_URL}/aircraft` with `Cookie: access_token=${token}` header and `cache: 'no-store'`
       - Pass the aircraft array to `<AircraftTable aircraft={data} />`
       - Use `API_URL` env var (server-only, no NEXT_PUBLIC_ prefix) — matches existing auth Server Action pattern

    2. Page layout:
       - Page title "Aircraft Fleet" in an h1 with text-gray-100 styling
       - Subtitle "Manage aircraft master data and cost parameters" in text-gray-400
       - `<AircraftTable>` component below

    Create `nextjs-project/src/components/aircraft/AircraftTable.tsx` as a "use client" component:

    1. Props: `aircraft: Aircraft[]` where Aircraft type matches the API list response
    2. State: `searchQuery` string for filter-as-you-type
    3. Search input at top: text input with placeholder "Search by MSN or registration...", AeroVista dark styling (bg-gray-800, border-gray-700, text-gray-100, placeholder-gray-500). Filter the aircraft array client-side using the search string (match against msn.toString() or registration, case-insensitive).
    4. Table with AeroVista dark styling:
       - Header row: MSN, Type, Registration, Lease Rent (USD), Lease Rent (EUR), 6Y Check (USD), 6Y Check (EUR), 12Y Check (USD), 12Y Check (EUR), LDG (USD), LDG (EUR)
       - Show USD and EUR side by side for each rate column per user decision
       - Each row clickable — wrap in Next.js `Link` to `/aircraft/${aircraft.msn}` or use `useRouter().push()`
       - Hover effect on rows (bg-gray-800/50 or similar)
       - Format monetary values with appropriate precision (2 decimal places for rates)
       - Empty state: if no aircraft match search, show "No aircraft found" message
    5. Styling: Match AeroVista dark theme — bg-gray-900 card, border-gray-800, text-gray-300 for data, text-gray-100 for headers
    6. No pagination needed (11 aircraft — per research discretion recommendation)

    Define the Aircraft TypeScript type either inline or in a separate types file. Include all fields from the list API response.
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Claud/nextjs-project && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Aircraft list page fetches from API and renders a searchable table with USD+EUR columns. Rows link to detail page. PlaceholderPage replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Aircraft detail page with cost sections and admin edit</name>
  <files>
    nextjs-project/src/app/(dashboard)/aircraft/[msn]/page.tsx,
    nextjs-project/src/components/aircraft/AircraftDetail.tsx,
    nextjs-project/src/components/aircraft/RatesSection.tsx,
    nextjs-project/src/components/aircraft/EprMatrixTable.tsx,
    nextjs-project/src/app/actions/aircraft.ts
  </files>
  <action>
    Create `nextjs-project/src/app/(dashboard)/aircraft/[msn]/page.tsx` as a Server Component:

    1. Dynamic route receiving `params: { msn: string }`
    2. Read cookie via `cookies()`, fetch `${API_URL}/aircraft/${msn}` with cookie forwarding
    3. If API returns 404, use `notFound()` from `next/navigation`
    4. Render breadcrumb: Aircraft > MSN {msn}
    5. Pass data to `<AircraftDetail aircraft={data} />`

    Create `nextjs-project/src/components/aircraft/AircraftDetail.tsx` as a "use client" component:

    1. Props: `aircraft: AircraftDetail` (full detail type matching API response), `isAdmin: boolean`
    2. Header section: MSN (large), aircraft type, registration (or "No registration" if null)
    3. Four sections arranged vertically (not tabs — all visible at once per research recommendation):
       a. **Fixed Monthly Rates** — `<RatesSection>` showing lease rent, 6Y check, 12Y check, LDG
       b. **Variable Rates (per engine)** — `<RatesSection>` showing APU, LLP #1, LLP #2
       c. **Escalation Rates** — Display EPR escalation, LLP escalation, AF+APU escalation as percentages
       d. **EPR Matrix** — `<EprMatrixTable>` showing cycle ratio x environment table

    Create `nextjs-project/src/components/aircraft/RatesSection.tsx` as a "use client" component:

    1. Props: `title: string`, `rates: {label: string, usd: number|string, eur: number|string, field: string}[]`, `msn: number`, `isAdmin: boolean`
    2. Display as a card (bg-gray-900 border border-gray-800 rounded-lg p-4)
    3. Each rate shown as a row: label, USD value, EUR value — side by side per user decision
    4. Admin edit: If `isAdmin`, show an "Edit" button per section. When clicked, fields become editable inputs (number type). A "Save" button triggers the `updateRatesAction` Server Action. A "Cancel" button reverts. Use immediate save per section per research recommendation.
    5. Format escalation rates as percentages: `(value * 100).toFixed(1)%`
    6. Loading state while saving (disable button, show spinner or "Saving...")
    7. Error display if update fails (show error message from Server Action response)

    Create `nextjs-project/src/components/aircraft/EprMatrixTable.tsx`:

    1. Props: `eprMatrix: {cycle_ratio: string, benign_rate: string, hot_rate: string}[]`, `msn: number`
    2. Display as a table inside a card: Columns are Cycle Ratio, Benign Rate, Hot Rate
    3. Rows rendered from the epr_matrix array (variable length per MSN — do NOT hardcode row count)
    4. Styling matches the main aircraft table dark theme
    5. Read-only for now — admin EPR editing is a discretion item and can use inline editing in a future enhancement within this phase if time allows, otherwise the basic display satisfies ACFT-03

    Create `nextjs-project/src/app/actions/aircraft.ts` with Server Actions:

    1. `updateRatesAction(msn: number, prevState: any, formData: FormData)`:
       - Read cookie from `cookies()`
       - Extract rate fields from formData, convert to numbers
       - PUT to `${API_URL}/aircraft/${msn}/rates` with JSON body
       - If success: `revalidatePath('/aircraft/' + msn)` and return `{success: true}`
       - If error: return `{error: data.detail}` (same pattern as loginAction)
    2. Use `'use server'` directive at top of file
    3. Use `API_URL` env var (server-only)

    For detecting admin status on the frontend: The getSession or auth/me response includes the user's role. Either:
    (a) Fetch /auth/me in the Server Component and pass `isAdmin` prop, or
    (b) Read from the session data if available.
    Use option (a): in the detail page Server Component, also fetch /auth/me and extract role === 'admin'. Pass isAdmin boolean to AircraftDetail.
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Claud/nextjs-project && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Detail page shows all 4 cost data sections. Admin can edit rates via Server Action. Non-admin sees read-only view. EPR matrix renders with variable row count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify aircraft list, detail, and admin edit UI</name>
  <files></files>
  <action>
    Human verifies the complete aircraft data management UI built in Tasks 1 and 2.
  </action>
  <what-built>
    Complete aircraft data management UI:
    1. Aircraft list page at /aircraft with searchable table showing all MSNs with USD+EUR columns
    2. Aircraft detail page at /aircraft/{msn} with fixed rates, variable rates, escalation rates, and EPR matrix sections
    3. Admin rate editing via inline edit with immediate save
  </what-built>
  <how-to-verify>
    Prerequisites: Both FastAPI (port 8000) and Next.js (port 3000) must be running. Database must be seeded with aircraft data (run: `python fastapi-project/scripts/seed_aircraft.py`).

    1. **Aircraft list page:**
       - Navigate to http://localhost:3000/aircraft
       - Verify table shows aircraft with MSN, type, registration columns
       - Verify USD and EUR rate columns are side by side
       - Type "3055" in search — verify only MSN 3055 appears
       - Clear search — verify all aircraft return
       - Type "LZ" in search — verify registration-based filtering works (if registrations are set)

    2. **Aircraft detail page:**
       - Click any aircraft row — should navigate to /aircraft/{msn}
       - Verify four sections visible: Fixed Monthly Rates, Variable Rates, Escalation Rates, EPR Matrix
       - Verify EPR matrix shows correct number of rows (varies per MSN)
       - Verify USD and EUR values shown side by side in rate sections

    3. **Admin edit (if logged in as admin):**
       - On detail page, click "Edit" on Fixed Monthly Rates section
       - Change Lease Rent value
       - Click "Save" — verify value updates without page reload
       - Refresh page — verify saved value persists

    4. **Non-admin view (if logged in as regular user):**
       - Navigate to aircraft detail — verify no Edit buttons visible
  </how-to-verify>
  <verify>Human visual and functional verification</verify>
  <done>User confirms aircraft list, detail, and admin edit all function correctly with proper styling.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- Aircraft list page replaces the PlaceholderPage (no more "arrives in Phase 2" text)
- Detail page renders all 4 sections with real data from API
- Server Action updates persist across page refresh
- No TypeScript errors in build
- AeroVista dark theme consistent across all new components
</verification>

<success_criteria>
- Aircraft list page shows all aircraft with USD+EUR columns and working search
- Aircraft detail page shows fixed rates, variable rates, escalation rates, EPR matrix
- Admin can edit and save rate changes from the detail page
- Non-admin sees read-only view without edit controls
- All pages match AeroVista dark theme styling
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/02-aircraft-master-data/02-03-SUMMARY.md`
</output>
