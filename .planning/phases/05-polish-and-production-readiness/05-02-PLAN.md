---
phase: 05-polish-and-production-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - nextjs-project/src/components/aircraft/AircraftTable.tsx
  - nextjs-project/src/components/quotes/QuoteList.tsx
  - fastapi-project/Dockerfile
  - fastapi-project/.dockerignore
  - fastapi-project/railway.json
  - fastapi-project/requirements.txt
  - vercel.json
autonomous: false
requirements:
  - UI-02
  - UI-04

must_haves:
  truths:
    - "User can sort AircraftTable by MSN and Type columns (ascending/descending toggle)"
    - "User can sort QuoteList by Quote #, Client, Status, and Created date columns"
    - "Tables are usable on mobile phone screens with non-essential columns hidden"
    - "Deployment configuration is ready for Vercel (frontend) and Railway (backend)"
  artifacts:
    - path: "nextjs-project/src/components/aircraft/AircraftTable.tsx"
      provides: "Sortable, responsive aircraft table"
      contains: "handleSort"
    - path: "nextjs-project/src/components/quotes/QuoteList.tsx"
      provides: "Sortable, responsive quote list"
      contains: "handleSort"
    - path: "fastapi-project/Dockerfile"
      provides: "Multi-stage Docker build for Railway deployment"
      contains: "gunicorn"
    - path: "fastapi-project/railway.json"
      provides: "Railway deployment configuration"
      contains: "healthcheckPath"
    - path: "vercel.json"
      provides: "Vercel monorepo root directory config"
      contains: "nextjs-project"
  key_links:
    - from: "AircraftTable.tsx"
      to: "User interaction"
      via: "onClick on th elements triggers sort state update"
      pattern: "onClick.*handleSort"
    - from: "QuoteList.tsx"
      to: "User interaction"
      via: "onClick on th elements triggers sort state update"
      pattern: "onClick.*handleSort"
    - from: "fastapi-project/Dockerfile"
      to: "Railway deployment"
      via: "gunicorn CMD binding to $PORT"
      pattern: "PORT:-8000"

user_setup:
  - service: railway
    why: "Backend hosting with managed PostgreSQL"
    env_vars:
      - name: DATABASE_URL
        source: "Auto-populated by Railway PostgreSQL add-on"
      - name: JWT_SECRET
        source: "Generate: openssl rand -hex 32"
      - name: ENVIRONMENT
        source: "Set to 'production'"
      - name: FRONTEND_URL
        source: "Your Vercel deployment URL (e.g., https://acmi-pricing.vercel.app)"
    dashboard_config:
      - task: "Create new project and add PostgreSQL add-on"
        location: "Railway Dashboard -> New Project -> Add PostgreSQL"
      - task: "Connect GitHub repo for auto-deploys"
        location: "Railway Dashboard -> Service -> Settings -> Source"
  - service: vercel
    why: "Frontend hosting with auto-deploys"
    env_vars:
      - name: API_URL
        source: "Your Railway backend URL (e.g., https://acmi-api.up.railway.app)"
    dashboard_config:
      - task: "Import GitHub repo, set Root Directory to 'nextjs-project'"
        location: "Vercel Dashboard -> Add New Project -> Import"
---

<objective>
Add column sorting and mobile responsiveness to data tables, configure deployment for Vercel and Railway, and perform final visual verification.

Purpose: UI-02 requires sortable, responsive data tables. The deployment configuration ensures the application is production-ready for Vercel (frontend) + Railway (backend + PostgreSQL). UI-04 (dashboard stats) was explicitly DROPPED by the user and is not implemented.

Output: Sortable AircraftTable and QuoteList with mobile responsiveness, Dockerfile and Railway config for backend, Vercel config for frontend, human verification of complete application.

NOTE: UI-04 (dashboard stats) is listed in requirements because it is assigned to this phase in the ROADMAP, but the user explicitly dropped it during /gsd:discuss-phase. No work is done for UI-04.
</objective>

<execution_context>
@/Users/abu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-production-readiness/05-CONTEXT.md
@.planning/phases/05-polish-and-production-readiness/05-RESEARCH.md
@.planning/phases/05-polish-and-production-readiness/05-01-SUMMARY.md

<interfaces>
<!-- AircraftTable and QuoteList will have been theme-converted by Plan 01. -->
<!-- Executor should read current state of these files before modifying. -->
<!-- Key interfaces for sorting: -->

From AircraftTable.tsx (after Plan 01 theme conversion):
```tsx
export interface Aircraft {
  id: number
  msn: number
  aircraft_type: string
  registration: string | null
  lease_rent_usd: string
  six_year_check_usd: string
  twelve_year_check_usd: string
  ldg_usd: string
  lease_rent_eur: string
  six_year_check_eur: string
  twelve_year_check_eur: string
  ldg_eur: string
}
// Currently has: useState for searchQuery, filter logic, table rendering
// Sorting needs to be added alongside existing search filter
```

From QuoteList.tsx (after Plan 01 theme conversion):
```tsx
interface QuoteListProps {
  initialQuotes: { items: QuoteListItem[]; total: number }
}
// Currently has: useState for search, statusFilter, debounced server-side fetch
// NOTE: QuoteList fetches data from server -- sorting should be CLIENT-SIDE on the fetched results
// QuoteListItem fields: id, quote_number, client_name, status, exchange_rate, msn_list, created_at
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add column sorting and mobile responsiveness to AircraftTable and QuoteList</name>
  <files>
    nextjs-project/src/components/aircraft/AircraftTable.tsx,
    nextjs-project/src/components/quotes/QuoteList.tsx
  </files>
  <action>
    **AircraftTable.tsx -- Sorting:**
    1. Add sort state: `const [sortKey, setSortKey] = useState<'msn' | 'aircraft_type'>('msn')` and `const [sortDir, setSortDir] = useState<'asc' | 'desc'>('asc')`
    2. Add `handleSort` function: if same key clicked, toggle direction; if different key, set key and reset to 'asc'
    3. After the existing `filtered` array, create `sorted`: `[...filtered].sort(...)` using sortKey/sortDir. For 'msn' compare numerically, for 'aircraft_type' use localeCompare.
    4. Replace `filtered.map(...)` with `sorted.map(...)` in the render
    5. Add `onClick={() => handleSort('msn')}` and `onClick={() => handleSort('aircraft_type')}` to the MSN and Type `<th>` elements. Add `cursor-pointer select-none` classes. Show sort indicator: unicode triangle (ascending: \u25B2, descending: \u25BC) next to the active sort column header text.
    6. Per user decision: Only MSN and Type are sortable. Rate columns are NOT sortable.

    **AircraftTable.tsx -- Mobile Responsive:**
    7. Add `hidden md:table-cell` to ALL rate column `<th>` and `<td>` elements (Lease Rent USD, Lease Rent EUR, 6Y Check USD, 6Y Check EUR, 12Y Check USD, 12Y Check EUR, LDG USD, LDG EUR). This hides 8 rate columns on mobile, showing only MSN, Type, Registration.
    8. Keep the existing `overflow-x-auto` wrapper on the table container.
    9. Add `min-w-[400px]` to the `<table>` element to prevent excessive squishing on very small screens.

    **QuoteList.tsx -- Sorting:**
    1. Add sort state: `const [sortKey, setSortKey] = useState<'quote_number' | 'client_name' | 'status' | 'created_at'>('created_at')` and `const [sortDir, setSortDir] = useState<'asc' | 'desc'>('desc')` (default: newest first)
    2. Add `handleSort` function (same toggle pattern as AircraftTable)
    3. Create `sortedQuotes` from the `quotes` state array. Sort logic:
       - 'quote_number': string localeCompare
       - 'client_name': string localeCompare (case-insensitive)
       - 'status': string localeCompare
       - 'created_at': Date comparison (new Date(a) - new Date(b))
    4. Replace `quotes.map(...)` with `sortedQuotes.map(...)` in the render
    5. Add onClick handlers to Quote #, Client, Status, and Created `<th>` elements. Add `cursor-pointer select-none` classes. Show sort indicator triangle.
    6. Per user decision: Rate and MSNs columns are NOT sortable.

    **QuoteList.tsx -- Mobile Responsive:**
    7. Add `hidden sm:table-cell` to Rate `<th>/<td>` and MSNs `<th>/<td>` columns (hide below sm breakpoint).
    8. Add `hidden sm:table-cell` to Actions column `<th>/<td>` (status dropdown less critical on mobile; status is still visible via StatusBadge).
    9. Keep overflow-x-auto wrapper. Add `min-w-[500px]` to `<table>`.

    IMPORTANT: Preserve ALL existing functionality (search filter, status filter, debounced fetch, status update). Sorting is additive.
  </action>
  <verify>
    <automated>cd nextjs-project && npm run build</automated>
  </verify>
  <done>
    - AircraftTable sorts by MSN (numeric) and Type (alpha) with direction toggle
    - QuoteList sorts by Quote #, Client, Status, Created date with direction toggle
    - Sort indicators (triangles) visible on active sort column
    - Rate columns hidden on mobile (below md) in AircraftTable
    - Rate, MSNs, Actions columns hidden on small screens (below sm) in QuoteList
    - Existing search/filter functionality fully preserved
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create deployment configuration for Railway and Vercel</name>
  <files>
    fastapi-project/Dockerfile,
    fastapi-project/.dockerignore,
    fastapi-project/railway.json,
    fastapi-project/requirements.txt,
    vercel.json
  </files>
  <action>
    **fastapi-project/Dockerfile:**
    Create a multi-stage Docker build:
    ```
    Stage 1 (builder): python:3.12-slim, create venv, pip install requirements.txt + gunicorn
    Stage 2 (runtime): python:3.12-slim, copy venv from builder, copy app/ migrations/ scripts/ directories
    ENV: PYTHONDONTWRITEBYTECODE=1, PYTHONUNBUFFERED=1, PATH with venv
    EXPOSE 8000
    CMD: gunicorn app.main:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind "0.0.0.0:${PORT:-8000}" --timeout 120
    ```
    See research "FastAPI Dockerfile for Railway" code example for exact implementation.

    **fastapi-project/.dockerignore:**
    ```
    __pycache__
    *.pyc
    .env
    .git
    tests/
    pytest.ini
    *.md
    .planning/
    ```

    **fastapi-project/railway.json:**
    ```json
    {
      "$schema": "https://railway.app/railway.schema.json",
      "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "./Dockerfile"
      },
      "deploy": {
        "healthcheckPath": "/health",
        "healthcheckTimeout": 300,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
      }
    }
    ```

    **fastapi-project/requirements.txt:**
    Add `gunicorn` to the existing requirements (append as last line). Do NOT modify existing entries.

    **vercel.json (repo root):**
    ```json
    {
      "$schema": "https://vercel.com/schema.json",
      "framework": "nextjs",
      "outputDirectory": ".next",
      "installCommand": "npm install",
      "buildCommand": "npm run build",
      "rootDirectory": "nextjs-project"
    }
    ```

    IMPORTANT: Verify that a `/health` endpoint exists in the FastAPI app. If not, add a simple GET /health route to `app/main.py` that returns `{"status": "ok"}`. Check by grepping for "health" in `app/main.py`.
  </action>
  <verify>
    <automated>cd fastapi-project && python3 -m pytest tests/ -x -q && cd ../nextjs-project && npm run build</automated>
  </verify>
  <done>
    - Dockerfile exists at fastapi-project/Dockerfile with multi-stage build, gunicorn CMD, $PORT binding
    - .dockerignore excludes tests, pycache, .env, .git
    - railway.json configures Dockerfile builder and health check at /health
    - gunicorn added to requirements.txt
    - vercel.json at repo root sets rootDirectory to nextjs-project
    - /health endpoint exists in FastAPI app
    - All existing backend tests pass
    - Frontend build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of theme, sorting, responsiveness, and deployment config</name>
  <files>none (verification only)</files>
  <action>
    Human verifies the complete Phase 5 implementation across both plans. This checkpoint covers:
    1. Dark/light/system theme toggle (Plan 01) -- theme persists in localStorage, all 38+ files converted
    2. Column sorting on AircraftTable (MSN, Type) and QuoteList (Quote #, Client, Status, Created)
    3. Mobile responsive tables with column hiding at breakpoints
    4. Deployment configuration (Dockerfile, railway.json, vercel.json)

    **Theme verification (critical -- most visual changes):**
    1. Start dev servers: `cd fastapi-project && uvicorn app.main:app --port 8000` and `cd nextjs-project && npm run dev`
    2. Open http://localhost:3000, log in
    3. Find the theme toggle in the sidebar footer
    4. Click "Light" -- verify ALL pages look correct with white/light backgrounds and dark text:
       - Dashboard (pricing inputs + summary)
       - P&L page
       - Aircraft list and detail
       - Crew page
       - Costs page
       - Sensitivity page (check chart readability)
       - Quotes list and detail
    5. Click "Dark" -- verify all pages look identical to before this phase (no regression)
    6. Click "System" -- verify it follows your OS setting
    7. Set to Light, refresh the page -- verify theme persists (no flash)
    8. Check the login page in light mode (log out first)

    **Sorting verification:**
    9. Go to Aircraft page -- click MSN header, verify sorting toggles. Click Type header, verify sorting.
    10. Go to Quotes page -- click Quote #, Client, Status, Created headers. Verify each sorts correctly.

    **Mobile verification:**
    11. Open browser DevTools, set viewport to iPhone (375px width)
    12. Check Aircraft table: only MSN, Type, Registration should be visible
    13. Check Quotes table: only Quote #, Client, Status, Created should be visible
    14. Verify tables are still scrollable horizontally

    **Deployment files (quick check):**
    15. Verify files exist: `fastapi-project/Dockerfile`, `fastapi-project/railway.json`, `vercel.json`
  </action>
  <verify>Human visual inspection -- type "approved" or describe issues</verify>
  <done>All theme, sorting, responsive, and deployment verification steps pass user inspection</done>
</task>

</tasks>

<verification>
1. `cd nextjs-project && npm run build` -- zero errors
2. `cd fastapi-project && python3 -m pytest tests/ -x -q` -- all tests pass
3. Visual inspection: all pages correct in both light and dark mode
4. Sorting works on specified columns in both tables
5. Mobile responsive: non-essential columns hidden at small breakpoints
</verification>

<success_criteria>
- AircraftTable sortable by MSN and Type with visual indicators
- QuoteList sortable by Quote #, Client, Status, Created with visual indicators
- Both tables responsive on mobile with column hiding
- Dockerfile, railway.json, .dockerignore, vercel.json all present and correct
- gunicorn in requirements.txt, /health endpoint exists
- Human verification passed for theme + sorting + responsive + deployment files
- UI-04 acknowledged as dropped (no implementation)
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-production-readiness/05-02-SUMMARY.md`
</output>
